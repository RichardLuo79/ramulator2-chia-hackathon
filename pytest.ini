[pytest]
# Upstream vendors include live-service integration tests. They are not part of
# the artifact's ordinary test command. Our fixtures use scripted providers.
testpaths = chia-loop/tests
norecursedirs = .git .work .data .venv* vendor ext
